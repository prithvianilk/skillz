interface:
  display_name: "Babysit PR"
  short_description: "Request reviews, monitor PR feedback and CI"
  default_prompt: "Use $babysit-pr to request Codex and CodeRabbit reviews, monitor the current PR and CI, then suggest any fixes."
